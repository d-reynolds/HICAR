<!-- Branch off the latest `main` and base this PR on `main` (trunk-based model).
     Make the title descriptive of what the PR is trying to accomplish. -->

## Summary
<!-- What does this PR solve/add to the code? -->


## Related issue
<!-- If you haven't already opened an issue for the fix/enhancement that this PR makes, please do so first. -->
Fixes #

## Type of change
- [ ] Bug fix
- [ ] New feature / enhancement
- [ ] Refactor or performance (no behaviour change)
- [ ] Documentation
- [ ] Build / CI / tooling
- [ ] Removal or deprecation

## Tests run
- [ ] Unit tests (`cd build && make check`)
- [ ] Test case suite (`cd build && make test_cases`)
- [ ] Reproducibility tests (`cd build && make test_reproducibility`)
- [ ] Regression test (`cd build && make test_regression`)
- [ ] Snowpack parity test (`cd build && make test_snowpack`)
- [ ] GPU parity test (`cd build && make test_gpu`)

## Model output (regression test result)
- [ ] Unchanged (`cd build && make test_regression`)
- [ ] Changed — intended (the regression baseline will need re-blessing after merge)
- [ ] Not applicable (docs / CI / tooling only)

## Checklist
- [ ] Branched off the latest `main` and targets `main`
- [ ] Linked to an issue
- [ ] Tests added/updated where it makes sense
- [ ] Docs updated if behaviour or interfaces changed
- [ ] Tests added if appropriate
